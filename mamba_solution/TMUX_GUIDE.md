# 🖥️ Руководство по tmux для обучения моделей

## Зачем нужен tmux?

**tmux** позволяет запустить обучение моделей и **выйти из терминала**, не прерывая процесс. Это критично важно для длительного обучения (1.5-3 часа).

### Преимущества:
- ✅ Обучение продолжается после выхода из SSH
- ✅ Можно подключиться к сессии с другого компьютера
- ✅ Логи сохраняются автоматически
- ✅ Можно запустить несколько процессов параллельно

---

## 📦 Установка tmux

Если tmux не установлен:

```bash
# Ubuntu/Debian
sudo apt-get update
sudo apt-get install tmux

# CentOS/RHEL
sudo yum install tmux

# macOS
brew install tmux
```

---

## 🚀 Быстрый старт

### Вариант 1: Базовое обучение (автоматические параметры)

```bash
./train_tmux.sh
```

Запустит обучение с параметрами по умолчанию:
- 3 модели
- 15 эпох
- batch_size=64
- d_model=128

### Вариант 2: Кастомные параметры (интерактивно)

```bash
./train_tmux_custom.sh
```

Позволит выбрать:
1. Быстрое тестирование (1 модель, 5 эпох)
2. Базовое обучение (3 модели, 15 эпох)
3. Улучшенное обучение (5 моделей, 20 эпох)
4. Свои параметры

### Вариант 3: Тестирование решения

```bash
./test_tmux.sh
```

Запустит `solution.py` в фоне.

---

## 📋 Основные команды tmux

### Управление сессиями

```bash
# Создать новую сессию
tmux new-session -s my_session

# Список всех сессий
tmux ls

# Подключиться к сессии
tmux attach-session -t mamba_training

# Удалить сессию
tmux kill-session -t mamba_training
```

### Горячие клавиши внутри tmux

Все комбинации начинаются с **Ctrl+B** (нажать и отпустить), затем следующая клавиша:

| Комбинация | Действие |
|------------|----------|
| `Ctrl+B`, затем `D` | Выйти из сессии (detach) |
| `Ctrl+B`, затем `C` | Создать новое окно |
| `Ctrl+B`, затем `N` | Следующее окно |
| `Ctrl+B`, затем `P` | Предыдущее окно |
| `Ctrl+B`, затем `[` | Режим прокрутки (выход: `Q`) |
| `Ctrl+B`, затем `?` | Помощь |

---

## 📊 Мониторинг обучения

### Способ 1: Подключиться к tmux сессии

```bash
tmux attach-session -t mamba_training
```

Увидите живой вывод обучения. Для выхода: **Ctrl+B, затем D**

### Способ 2: Смотреть логи

```bash
# В реальном времени
tail -f training.log

# Последние 50 строк
tail -n 50 training.log

# Поиск по логам
grep "Epoch" training.log
grep "Val Loss" training.log
```

### Способ 3: Проверить статус сессии

```bash
# Список сессий с информацией
tmux ls

# Пример вывода:
# mamba_training: 1 windows (created Sat Nov 17 10:30:15 2025)
```

---

## 💡 Типичные сценарии

### Сценарий 1: Запустить обучение и уйти

```bash
# Шаг 1: Запустить обучение в tmux
./train_tmux.sh

# Шаг 2: Дождаться начала обучения (увидите первую эпоху)

# Шаг 3: Выйти из tmux
# Нажмите: Ctrl+B, затем D

# Шаг 4: Можно закрыть терминал или выйти из SSH
exit
```

Обучение продолжится!

### Сценарий 2: Проверить прогресс через час

```bash
# Шаг 1: Подключиться к серверу по SSH (если вышли)

# Шаг 2: Посмотреть логи
cd /home/user/challenge/mamba_solution
tail -n 100 training.log

# Или подключиться к сессии
tmux attach-session -t mamba_training
```

### Сценарий 3: Запустить несколько экспериментов

```bash
# Запустить первый эксперимент
tmux new-session -d -s exp1 -n "3models_15ep"
tmux send-keys -t exp1 "cd /home/user/challenge/mamba_solution" C-m
tmux send-keys -t exp1 "python train.py --num-models 3 --epochs 15" C-m

# Запустить второй эксперимент
tmux new-session -d -s exp2 -n "5models_20ep"
tmux send-keys -t exp2 "cd /home/user/challenge/mamba_solution" C-m
tmux send-keys -t exp2 "python train.py --num-models 5 --epochs 20" C-m

# Посмотреть список
tmux ls

# Подключиться к нужному
tmux attach-session -t exp1
```

### Сценарий 4: Остановить обучение

```bash
# Вариант 1: Удалить сессию (жесткое прерывание)
tmux kill-session -t mamba_training

# Вариант 2: Подключиться и нажать Ctrl+C
tmux attach-session -t mamba_training
# Затем внутри: Ctrl+C
```

---

## 🔍 Устранение проблем

### Проблема: "failed to connect to server"

```bash
# Решение: Запустить tmux server
tmux start-server
```

### Проблема: Сессия "зависла"

```bash
# Просмотр всех сессий
tmux ls

# Удалить зависшую сессию
tmux kill-session -t mamba_training

# Запустить заново
./train_tmux.sh
```

### Проблема: "command not found: tmux"

```bash
# Установить tmux
sudo apt-get update && sudo apt-get install tmux
```

### Проблема: Потерялся лог-файл

```bash
# Найти все логи обучения
find /home/user/challenge/mamba_solution -name "training*.log"

# Посмотреть самый свежий
ls -lt /home/user/challenge/mamba_solution/*.log | head -1
```

---

## 🎓 Продвинутые фичи

### Разделение экрана (split panes)

```bash
# Внутри tmux сессии:
# Горизонтальное разделение: Ctrl+B, затем "
# Вертикальное разделение: Ctrl+B, затем %

# Переключение между панелями: Ctrl+B, затем стрелки
# Закрыть панель: exit или Ctrl+D
```

### Прокрутка вывода

```bash
# Внутри tmux сессии:
# Ctrl+B, затем [
# Теперь можно прокручивать стрелками или Page Up/Down
# Выход: Q
```

### Сохранение истории в файл

```bash
# Внутри tmux сессии:
# Ctrl+B, затем :
# Ввести: capture-pane -S -3000
# Затем: save-buffer training_history.txt
```

---

## 📝 Полезные алиасы

Добавьте в `~/.bashrc` для удобства:

```bash
# Алиасы для tmux
alias tls='tmux ls'
alias tat='tmux attach-session -t'
alias tks='tmux kill-session -t'
alias tlog='tail -f training.log'

# Использование:
# tls                    # список сессий
# tat mamba_training     # подключиться
# tks mamba_training     # удалить
# tlog                   # смотреть логи
```

После добавления:
```bash
source ~/.bashrc
```

---

## ⚡ Быстрая шпаргалка

### Запуск

```bash
./train_tmux.sh              # Базовое обучение
./train_tmux_custom.sh       # С выбором параметров
./test_tmux.sh               # Тестирование
```

### Управление

```bash
tmux ls                      # Список сессий
tmux attach -t mamba_training  # Подключиться
Ctrl+B, D                    # Выйти (detach)
tmux kill-session -t mamba_training  # Удалить
```

### Мониторинг

```bash
tail -f training.log         # Смотреть логи
tmux attach -t mamba_training  # Живой вывод
```

---

## 🎯 Рекомендации

1. **Всегда используйте tmux** для длительного обучения
2. **Проверяйте логи** регулярно (каждые 30-60 минут)
3. **Используйте осмысленные имена** для сессий (exp1, baseline, etc.)
4. **Сохраняйте логи** для анализа результатов
5. **Удаляйте старые сессии** после завершения обучения

---

## 📚 Дополнительная информация

### Официальная документация tmux
https://github.com/tmux/tmux/wiki

### Интерактивная шпаргалка
https://tmuxcheatsheet.com/

### Видео-туториалы
- https://www.youtube.com/watch?v=BHhA_ZKjyxo (tmux basics)
- https://www.youtube.com/watch?v=Lqehvpe_djs (tmux tutorial)

---

**Удачи в обучении моделей! 🚀**
