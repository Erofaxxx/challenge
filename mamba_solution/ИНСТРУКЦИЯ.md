# 🇷🇺 ИНСТРУКЦИЯ ПО ЗАПУСКУ (на русском языке)

## 📥 Шаг 1: Получение данных

Данные для соревнования нужно скачать с официального сайта:

1. Перейдите на https://wundernn.io/
2. Зарегистрируйтесь (sign up)
3. Перейдите в раздел Quick Start
4. Скачайте файл `train.parquet`
5. Создайте директорию `datasets` в корне проекта:
   ```bash
   mkdir -p /home/user/challenge/datasets
   ```
6. Поместите файл `train.parquet` в эту директорию:
   ```bash
   # Должно получиться: /home/user/challenge/datasets/train.parquet
   ```

---

## 🔧 Шаг 2: Установка зависимостей

```bash
cd /home/user/challenge/mamba_solution
pip install -r requirements.txt
```

Это установит:
- PyTorch (библиотека для нейросетей)
- NumPy (работа с массивами)
- Pandas (работа с данными)
- scikit-learn (метрики)
- tqdm (прогресс-бары)
- pyarrow (чтение parquet файлов)

---

## 🎓 Шаг 3: Обучение моделей

### ⚡ РЕКОМЕНДУЕТСЯ: Обучение в tmux (можно выйти из терминала)

**tmux** позволяет запустить обучение и выйти - процесс продолжится в фоне!

```bash
# Вариант 1: Автоматическое обучение (3 модели, 15 эпох)
./train_tmux.sh

# Вариант 2: Выбор параметров (интерактивно)
./train_tmux_custom.sh
```

**После запуска:**
1. Увидите начало обучения
2. Нажмите **Ctrl+B, затем D** для выхода
3. Можете закрыть терминал - обучение продолжится!

**Подключиться обратно:**
```bash
tmux attach-session -t mamba_training
```

**Посмотреть логи:**
```bash
tail -f training.log
```

📖 Подробнее: смотрите `TMUX_GUIDE.md`

---

### Базовое обучение БЕЗ tmux (не рекомендуется для долгого обучения)

```bash
python train.py --data ../datasets/train.parquet
```

**Что происходит:**
- Загружаются данные из файла
- Обрабатываются 500 последовательностей
- Обучаются 3 модели с разными random seeds
- Каждая модель обучается 15 эпох
- Модели сохраняются в файлы `mamba_model_0.pt`, `mamba_model_1.pt`, `mamba_model_2.pt`

**Ожидаемое время:** 1.5-3 часа (зависит от процессора)

**⚠️ ВНИМАНИЕ:** Если закроете терминал - обучение прервется!

### Быстрое обучение (для теста)

Если хотите быстро проверить что всё работает:

```bash
# С tmux (рекомендуется)
./train_tmux_custom.sh
# Выберите опцию 1 "Быстрое тестирование"

# Или без tmux
python train.py \
    --data ../datasets/train.parquet \
    --num-models 1 \
    --epochs 5 \
    --batch-size 32
```

**Ожидаемое время:** 15-30 минут

---

## ✅ Шаг 4: Тестирование решения

После обучения запустите:

```bash
python solution.py
```

**Что происходит:**
- Загружаются обученные модели
- Прогоняются все данные
- Вычисляется R² score (метрика качества)
- Показываются результаты

**Ожидаемый результат:**
```
Загружаем 3 обученных моделей...
  Загружена модель: mamba_model_0.pt
  Загружена модель: mamba_model_1.pt
  Загружена модель: mamba_model_2.pt

Средний R² по всем признакам: 0.XXXXXX
```

**Целевой R² score: ≥ 0.39** (чем выше, тем лучше)

---

## 📦 Шаг 5: Создание файла для отправки

```bash
./prepare_submission.sh
```

Или вручную:

```bash
zip submission.zip solution.py mamba_model_*.pt
```

**Что получится:**
- Файл `submission.zip` с вашим решением
- Внутри: `solution.py` и файлы весов моделей

---

## 🚀 Шаг 6: Отправка решения

1. Перейдите на сайт соревнования: https://wundernn.io/
2. Войдите в свой аккаунт
3. Перейдите в раздел Submit
4. Загрузите файл `submission.zip`
5. Дождитесь результатов оценки

**Ограничения:**
- Максимум 10 отправок в день
- Время выполнения: до 60 минут
- Только CPU (без GPU)

---

## 📊 Понимание результатов

### R² Score (коэффициент детерминации)

Это метрика качества предсказаний:
- **R² = 1.0** — идеальные предсказания
- **R² = 0.0** — предсказания на уровне среднего значения
- **R² < 0.0** — предсказания хуже среднего

**Типичные результаты:**
- Простое среднее: R² ≈ 0.15-0.20
- LSTM: R² ≈ 0.25-0.35
- Mamba-2 (наше решение): R² ≈ 0.39-0.45

---

## 🔧 Настройка параметров

### Если мало памяти (RAM < 16GB)

```bash
python train.py \
    --data ../datasets/train.parquet \
    --num-models 2 \
    --d-model 96 \
    --batch-size 32 \
    --epochs 12
```

### Если много памяти (RAM > 32GB)

```bash
python train.py \
    --data ../datasets/train.parquet \
    --num-models 5 \
    --d-model 192 \
    --n-layers 6 \
    --batch-size 128 \
    --epochs 20
```

### Если нужно быстрее обучить

```bash
python train.py \
    --data ../datasets/train.parquet \
    --num-models 1 \
    --epochs 8 \
    --batch-size 32
```

---

## 📋 Параметры командной строки

### train.py параметры:

| Параметр | Описание | По умолчанию |
|----------|----------|--------------|
| `--data` | Путь к файлу с данными | `../datasets/train.parquet` |
| `--num-models` | Количество моделей в ансамбле | 3 |
| `--d-model` | Размерность модели (влияет на качество и скорость) | 128 |
| `--n-layers` | Количество слоев Mamba | 4 |
| `--batch-size` | Размер батча (больше = быстрее, но нужно больше памяти) | 64 |
| `--epochs` | Количество эпох обучения | 15 |
| `--lr` | Learning rate (скорость обучения) | 0.0005 |
| `--context-length` | Длина контекста в шагах | 80 |

---

## 🐛 Решение типичных проблем

### Ошибка: "No module named 'torch'"

```bash
pip install torch numpy pandas scikit-learn tqdm pyarrow
```

### Ошибка: "File not found: train.parquet"

Убедитесь что файл находится по пути: `/home/user/challenge/datasets/train.parquet`

```bash
ls -la /home/user/challenge/datasets/train.parquet
```

### Ошибка: "Out of memory" (нехватка памяти)

Уменьшите параметры:

```bash
python train.py \
    --data ../datasets/train.parquet \
    --batch-size 16 \
    --d-model 64 \
    --num-models 1
```

### Обучение идет слишком долго

Уменьшите количество эпох и моделей:

```bash
python train.py \
    --data ../datasets/train.parquet \
    --num-models 1 \
    --epochs 5
```

### Низкий R² score

Попробуйте:
1. Обучить больше моделей: `--num-models 5`
2. Обучить дольше: `--epochs 20`
3. Увеличить размер модели: `--d-model 192 --n-layers 6`

---

## 📈 Улучшение результатов

### 1. Увеличение ансамбля

Больше моделей = более стабильные предсказания:

```bash
python train.py --num-models 5
```

### 2. Более глубокая модель

Больше слоев = более сложные паттерны:

```bash
python train.py --n-layers 6 --d-model 192
```

### 3. Больше обучения

Больше эпох = лучшее качество (но есть риск переобучения):

```bash
python train.py --epochs 20
```

---

## 📁 Структура файлов после обучения

```
mamba_solution/
├── solution.py              ← Главный файл (обязательный!)
├── train.py                 ← Скрипт обучения
├── requirements.txt         ← Зависимости
├── README.md               ← Инструкция на английском
├── ИНСТРУКЦИЯ.md           ← Эта инструкция
├── prepare_submission.sh   ← Скрипт создания архива
├── mamba_model_0.pt        ← Обученная модель 1
├── mamba_model_1.pt        ← Обученная модель 2
├── mamba_model_2.pt        ← Обученная модель 3
└── submission.zip          ← Готовый файл для отправки
```

---

## 🎯 Чек-лист перед отправкой

Убедитесь что:

- ✅ Данные скачаны и находятся в `../datasets/train.parquet`
- ✅ Установлены все зависимости (`pip install -r requirements.txt`)
- ✅ Обучение завершилось успешно (есть файлы `mamba_model_*.pt`)
- ✅ Тестирование прошло (`python solution.py` работает)
- ✅ R² score ≥ 0.39
- ✅ Создан файл `submission.zip`
- ✅ В архиве есть `solution.py` и файлы `mamba_model_*.pt`

---

## 💡 Советы для победы

1. **Обучите много моделей** (5-7 штук) — ансамбль всегда лучше
2. **Экспериментируйте с параметрами** — попробуйте разные d_model и n_layers
3. **Следите за валидационной ошибкой** — она должна уменьшаться
4. **Не переобучайтесь** — если val loss растет, уменьшите epochs
5. **Проверяйте на локальных данных** — перед отправкой всегда тестируйте

---

## ❓ Частые вопросы

**Q: Сколько времени займет обучение?**
A: 1.5-3 часа для 3 моделей по 15 эпох на среднем CPU.

**Q: Можно ли обучить на GPU?**
A: Да, код автоматически определит GPU если он есть. Но для отправки модель должна работать на CPU.

**Q: Что делать если R² < 0.39?**
A: Попробуйте обучить больше моделей или увеличить d_model.

**Q: Можно ли изменять solution.py?**
A: Да, но структура класса PredictionModel должна остаться такой же.

**Q: Что если у меня нет файла train.parquet?**
A: Скачайте его с сайта соревнования после регистрации.

---

## 🏆 Удачи в соревновании!

Если что-то непонятно или не работает:
1. Перечитайте эту инструкцию
2. Проверьте раздел "Решение типичных проблем"
3. Убедитесь что все файлы на месте

**Желаем высокого R² score и победы! 🚀**
